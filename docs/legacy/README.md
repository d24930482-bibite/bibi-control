# Legacy Docs

This folder keeps pre-implementation notes and handoff documents for historical context.

These files may describe planned work that has already landed or proposed shapes that changed during implementation. Prefer the current docs in `docs/` for active parser and normalization behavior.
