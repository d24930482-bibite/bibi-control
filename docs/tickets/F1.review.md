# F1 review — cross-world transfer: graft mechanics + fail-loud species handling

Reviewer: independent. Verdict at bottom. Code under review:
`/home/asemones/Documents/personal_code/bibicontrol-wt/F1`, diff vs `main`.

## Scope of the re-scoped F1
The one behavioral change vs the original (buggy) F1: species handling flips from
silent-add (`ensureActiveSpecies`) to fail-loud refusal; real remap deferred to F3.
Reviewed against `docs/workspace_plan.md#f1`, the F1 plan, and the 6 stake-the-merge
points.

## What I verified

1. NO SILENT CONFLATION REMAINS.
   `grep -rn ensureActiveSpecies` over the whole worktree returns nothing — the silent-add
   helper is gone with no caller. `reconcileGraftIdentity` (transfer_identity.go:46) routes
   the species check through `refuseSpeciesGraft`, which NEVER mutates `activeSpeciesList`,
   never fabricates a record, never adopts a same-id dest species. It only returns errors.

2. HEADLINE TEST IS REAL — and proven by mutation.
   Dest fixture `parseSpeciesArchive` (append_delete_test.go:533): activeSpeciesList [1,2,3],
   bibite_1/bibite_2 (bodies 43/44) BOTH species 2, egg species 3. The headline subtest
   "present colliding linear id refuses, NOT conflates" retags source bibite_0 (body 500) to
   species 2 — a genuine collision against the biologically-different dest species 2. Asserts
   refusal naming species 2 + "conflate", no `bibites/bibite_3.bb8` appended, 0 staged ops,
   activeSpeciesList unchanged [1,2,3].
   MUTATION CHECK: I temporarily neutralized `refuseSpeciesGraft` to a no-op (the old
   silent-adopt behavior) in a scratch copy. The headline subtest, the absent subtest, and
   the egg subtest all FAILED ("error = nil, want loud refusal"); the species-less subtest
   stayed green (correct — it is the non-refusing path). File restored byte-identical
   afterward; suite re-confirmed green. This is the exact blind spot the original F1 missed,
   and the new test catches it.

3. REFUSAL IS ATOMIC — by construction, not by Apply-unwind.
   `AppendEntry` (transfer.go) deep-clones, then calls `reconcileGraftIdentity` (the pure
   check, incl. species) BEFORE `nextEntryName` and `StageAppendBibite`. A refused graft
   never reaches staging, so 0 ops by construction. Tests assert
   `len(dst.StagedOperations()) == 0`, `dst.State() == StateClean`, and activeSpeciesList ==
   [1,2,3] via the shared `assertDestUnstaged` helper across all refusal subtests.

4. BOTH ERRORS DISTINCT + EGG PATH.
   `refuseSpeciesGraft` emits three distinct named errors: present-in-dest (conflation risk,
   names "conflate" + the id + "per-world linear"), absent-from-dest (names "absent" + import
   + F3), and a third for a dest with no decoded species table (refuses rather than grafting
   into a save with no usable species table — a reasonable, documented conservative choice).
   `dstEntityHasSpecies` adds a belt-and-suspenders "present" detector over live dest entities
   beyond activeSpeciesList. Egg path: `reconcileGraftIdentity` runs the species check for
   both EntryBibite and EntryEgg; the egg subtest (species 8, absent) refuses and stages 0.

5. KEPT MECHANICS STILL CORRECT AND TESTED.
   body.id collision guard (`dstBibiteWithBodyID`), dangling child-ref rejection
   (`danglingChildRefs`/`dstBibiteBodyIDs`), `nextEntryName` fresh max+1 non-colliding name,
   kind re-validation at apply (`TestTransferAppendEntryWrongKindName`), deep clone before
   reconcile, and the SQL-ref resolver seams (settings copy, all array tables via
   `CollectArrayElement`→`SQLDelete` guard, pellet SceneCount reconciliation) all retained and
   green. `TestTransferAppendEntryRoundTrip` correctly re-pointed at a species-less source
   bibite so it proves mechanics without tripping the new refusal, doubling as the
   species-less-grafts-cleanly boundary case.

6. NO SPEC DRIFT.
   Refusing ALL species-bearing grafts is the deliberate conservative split; F3 lifts it via
   remap (per `docs/workspace_plan.md#f1` "phase it, fail loudly on unhandled cases" and the
   key-risks note). The plan faithfully covers the source spec; the conservative refusal is
   intended, not a dropped requirement, and test comments warn future readers not to "fix" it
   back into an add.

## Scope boundary / hygiene
- Only `saveparser/thebibites` imported in transfer.go / transfer_identity.go; workspace.go
  prose-only. No `duckdb`/`revisionstore`/`blobstore`/`database/sql`/`workspace` import.
- F1 stops at staging on the dest Session; no commit/head advance.
- Stale prose updated (transfer_identity.go header, transfer.go AppendEntry doc + method map,
  workspace.go interface doc) to state the fail-loud rule and point to F3.
- `go vet ./savemutator/thebibites/` clean.

## Perf
Plan carries no Perf-review flag; the dest-scan helpers are O(dest entries) over an in-memory
archive invoked once per graft (not a hot path, no N+1, no growing-data quadratic). Diff did
not surprise. Skipped per policy.

## Test results
`go test ./...` green (whole module). Targeted transfer tests green with `-count=1`.
Broader gate `go test ./savemutator/... ./saveparser/... ./script/...` green.

VERDICT: PASS
