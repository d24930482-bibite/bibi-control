# H4 review — drop the `json_scalars` EAV (stop building 99.4%-discarded scalars + remove the unread table)

Reviewer: independent. Code under review: worktree `bibicontrol-wt/H4` @ `b447a53`, diffed
against BASE `main` = `67b6a22` (the H2 merge — confirmed identical commit, so "vs main" and
"vs H2 base" are the same).

## What I verified independently

### #1 GOLDEN PROVENANCE — PROVEN pre-H4 (the decisive check)
`typed_projection_identity_test.go` pins a sha256 fingerprint (count:hash) per normalized table
and asserts every typed table is byte-identical. A self-generated golden would prove nothing, so
I reconstructed the pre-H4 output independently:
- `git worktree add --detach /tmp/h4-base 67b6a22` (pre-H4 code: `json_scalars.go` present,
  `JSONScalars` field present).
- Dropped a probe using the EXACT same reflect-over-`dbtable` fingerprint function and the same
  fixture (`autosave_20260301021357.zip`) + same saveID (`"identity"`), ran it on the OLD code.
- Result: all **43** typed-table goldens in the H4 test match the PRE-H4 base output
  **byte-for-byte** (programmatic diff: zero mismatches). The base additionally emits
  `json_scalars: 5332:...` — exactly the 5,332 kept rows from the ticket's diagnosis — which the
  H4 goldens correctly omit because it's the dropped table.
- Ran the H4 test against the NEW code: PASS. So post-H4 output == goldens == pre-H4 output.
The identity guarantee is REAL, not self-fulfilling. Scratch worktree removed (`git worktree remove`).

### 1. No typed-data loss
The identity fingerprints cover every typed table and all match pre-H4. Spot-check of populated
gene/settings/body tables (from the goldens, all > 0): bibite_genes 40091, species_genes 1085,
egg_genes 314, bibite_body 1027, settings_simulation_values 77, settings_independent_values 14,
settings_material_values 30. The deleted helpers (`parseBibiteGenes`/`parseBibiteClock`/
`parseEggGenes`/`parseEggState`/template-genes block) only fed the EAV; typed gene rows come from
`normalizeSpecies`/`appendGeneRowsFromMap` (unchanged), confirmed by the identical species_genes /
bibite_genes fingerprints. `eggState` id read survives in `parseEgg`. `ScalarType` + its constants
survive and still feed the typed value path (script/thebibites convert/guards/settings_value/zones);
the bare `Scalar` struct is fully removed with zero remaining references.

### 2. No production reader
Grepped all of `script/ workspace/ savemutator/` + generated sqlref metadata for
`json_scalars`/`JSONScalars`/`ScalarRow`/`"scalars"` collection kind: zero. The only surviving
references are the DROP migration, comments, and the new tests. `import.go` was correctly NOT
touched — `json_scalars` auto-left `NormalizedTables`/`allTables` via the metadata regen.

### 3. Churn visibility — fires correctly, no spurious noise
`unknown_json_diagnostic_test.go` is meaningful and I confirmed it: an unknown `.bb8scene` object
section emits `unknown_json_section` (Warning) and survives ExtractTables into the diagnostics
table; a known `vars` section does NOT trip it; a non-object unknown section gets the existing
`json_not_object` and NOT the new code; a fully-normal save produces ZERO of the new diagnostic at
both archive and normalized level. `future.bb8scene` correctly classifies as EntryUnknownJSON.

### 4. Migration drops the table on an EXISTING db
`ApplyMigrations` runs every `*.sql` in name order on each open (no version tracking), so 0002
runs on every persistent DB. I seeded a DB with a pre-existing `json_scalars` table + a row, ran
the REAL embedded `ApplyMigrations`, and confirmed the table is dropped (count 0 in
`duckdb_tables()`) and that a second `ApplyMigrations` is idempotent. `IF EXISTS` makes it a no-op
on a fresh DB. (Reviewer probe removed after running.)

### 5. Perf — measured before/after on the fixture (medians of 3, ParseFile+ExtractTables)
- wall:   1027 ms -> 466 ms  (54.7% drop)
- bytes:  880 MB -> 387 MB   (56.0% drop)
- allocs: 7.77M  -> 4.60M    (40.8% drop)
Material on every axis; meets/exceeds the executor's claims (bytes ~56% and allocs ~41% match
exactly; wall even better). This is the allocation/GC-relief win the perf-review lens expected
(removing the ~864K discarded Scalar builds + per-leaf json.Marshal/strconv from the parallel
parse). No N+1 / quadratic / hot-path regression introduced — the change is pure work removal.

## Generated-file discipline & build
`go generate ./saveparser/thebibites` produces ZERO diff against the committed
`normalize_metadata.go` / `0001_extracted_save.sql` / `sqlref_generated.go` — no hand-editing.
The only hand edit in the gen pkg (removing the stale `"ScalarRow"` override) is present.
`0001` keeps the `bibite_mutation_refs` view after dropping the CREATE block. `go build ./...`
and `go vet ./saveparser/...` clean. Test edits are surgical — only the scalar-specific
assertions removed; all other count assertions preserved; `scalar_redundancy_test.go` (only
removed machinery) deleted; `TestDumpJSONScalarPaths` + the `JSONScalars` sample field removed
from `import_test.go`.

## Suite
- `go test -race -count=1 -timeout 20m ./saveparser/...`: clean (152s, no RACE).
- `go test -timeout 20m ./...`: GREEN — every package `ok` (incl. workspace), zero FAIL/panic.
- H4's new tests (TestTypedProjectionIdentity, TestUnknownJSON*, TestImportExtractedSaveCoversEveryTable)
  all PASS against the new code.

## Spec/plan faithfulness
The H4 spec lives in the ticket notes (measured probe + GOAL A/B + the two HARD constraints +
PERF-REVIEW a-e). The plan covers every requirement with no dropped/narrowed/contradicted item,
and the implementation matches the plan. The consciously-dropped fields (5 keep-suffixes with no
typed column + un-typed root scalars) are correctly the only data loss, and the identity test
proves nothing typed was lost.

Nothing failed any non-negotiable. The implementation is correct, complete, well-tested, and the
perf win is real and measured.

VERDICT: PASS
