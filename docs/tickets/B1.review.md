# B1 Review — LoadedSave seam: explicit world id + injected DB

## Scope
Diff (`main...HEAD`) touches exactly the two planned files plus the harness
`.current-ticket` marker:
- `script/thebibites/loadedsave.go` — `LoadInto` + private `newLoadedSave` builder + doc updates.
- `script/thebibites/loadedsave_seam_test.go` (new) — four seam tests.

No changes to `mirror.go`, `sql.go`, `run.go`, `bindings.go`, `duckdb/` — matches the plan.

## Correctness
- **Shared builder extraction is faithful.** `newLoadedSave` sets every field the old `Load`
  literal set (`path`, `saveID`, `archive`, `tables: ExtractTables(saveID, archive)`, `session`,
  `willCommit: true`) plus `db`, then calls `buildAccess()`. `Load` still derives `saveID` as
  `archive.SHA256` with `archive.FileName` fallback and delegates with `db: nil`. Standalone
  behavior is byte-identical.
- **`LoadInto` keys by worldID, not the hash.** `ExtractTables(worldID, archive)` stamps the
  in-memory rows and `tables.Archive.SaveID` with `worldID`, matching the key the mirror flush
  (`mirror.go:171-172`, `save_id = ls.saveID`) and query filters use. This is the consistency
  requirement in the plan; the test asserts `ls.tables.Archive.SaveID == "world-1"`.
- **Fail-loud validation.** Empty `worldID` and nil `db` are rejected before parse with wrapped
  errors, consistent with the surrounding `%q`/`%w` style and the churn fail-loud stance.
- **Injected DB short-circuit unchanged.** `openDB` early-returns `ls.db` before the import branch
  and before `dbOpenCount++`; the increment was not moved. The injected path leaves
  `dbOpenCount == 0`. No product code closes `ls.db`.

## Tests (real and meaningful)
- `TestLoadIntoSetsWorldIDAndInjectsDB` — asserts pointer identity (`ls.db == db`), key stamping,
  row count via the injected handle equals in-memory `len(ls.tables.Bibites)`, and
  `dbOpenCount == 0`.
- `TestLoadIntoMirrorRoundTripScopedToWorld` — drives a real `SetField("energy")` through the
  mirror, reads it back via `query` (which calls `flushMirror`), and proves a sibling `world-2`
  partition in the same shared handle is untouched (mirror scoping isolates worlds).
- `TestLoadIntoRejectsEmptyWorldOrNilDB` — both rejection paths.
- `TestLoadStillDerivesHashKeyAndLazyOpens` — guards the standalone hash-key + lazy in-memory open
  (`dbOpenCount == 1`), beyond the plan minimum.
None are weakened to pass; they pin the seam's contract precisely.

## Test runs (clean, `-count=1`)
- Focused: `TestLoadInto*`, `TestLoadStillDerivesHashKeyAndLazyOpens`, `TestRunAndCommitChurn` (churn DoD) all PASS.
- Full `go test ./...` — all packages PASS.

VERDICT: PASS
