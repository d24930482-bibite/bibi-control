# M8 Review — Save-format drift capture & write-path hardening

Reviewer: independent (opus). Worktree: `bibicontrol-wt/M8`. Base: `main`.
Diff touches 6 files (4 product/test-helper + 2 tests), 228 insertions:
`savemutator/thebibites/sqlref_entities.go`, `script/thebibites/gene.go`,
`saveparser/thebibites/parse_scene.go`, `duckdb/sqlref_scan_test.go` (test helper),
`savemutator/thebibites/sqlref_test.go`,
`saveparser/thebibites/tests/scene_version_diagnostic_test.go`.

## Spec ↔ plan ↔ implementation

Source spec `docs/missing.md#8`. Plan faithfully scopes the two correctness/observability
edges (gene-SET value-type guard = §8 bullet 4; scene version gate = §8 bullet 2) into v1
and explicitly defers the rest (unknown-section capture already mitigated by H4; cascade
atomicity / intermediate-structure as documented assumptions). Implementation matches the
plan one-for-one; no drift, no narrowed/contradicted requirement.

## 1. Guard correctness — PASS

`resolveGeneColumn` now captures `wantType` from `geneValueColumnTypes`
(`number_value`→`number`, `bool_value`→`bool`, `string_value`→`string`) and enforces
`requireSQLRefValueType(ref, wantType)` after the existing path check — exactly mirroring
the settings resolver. `setGeneValue` (`gene.go:159`) sets `ValueType: string(row.Type)`;
`column` is derived from the same `row.Type` via `scalarValueColumn`, and `ScalarType`
values (`"number"`/`"bool"`/`"string"`) are identical to the column-type map values, so the
live ref is guaranteed consistent and keeps passing. A `null`-typed gene fails at
`scalarValueColumn` before a ref is even built, so the empty-value_type rejection only bites
malformed direct callers (the intent).

Mutation-checked: with the guard block removed, all five `TestResolveGeneColumnTypeMismatch`
subcases (number↔string, number↔bool, string↔number, egg-gene, typeless) go red
(`error = nil, want "value_type"`). The test genuinely proves the fix; it is not a
same-type tautology. Restored the product file cleanly (no diff vs HEAD afterward).

## 2. Shared-resolver blast radius (the key risk) — PASS

Audited every production path that builds/scans a gene `SQLValueRef`:
- The ONLY production gene-ref constructor is `setGeneValue` (`gene.go`) — fixed to set
  `ValueType`. `grep` for `bibite_genes`/`egg_genes` in `script/` non-test code finds only
  `geneTable()`.
- The DSL bulk-write path (`bulkSet`/`bulkSetExpr` → `duckdb.ScanSQLRefs`) is driven by
  `attrRegistry()`/`entityTables`, whose tables are `bibites`, `bibite_body`, `bibite_mouth`,
  `bibite_pheromone_emitters`, `bibite_egg_layers`, `bibite_control`, `eggs`, `pellets` —
  never the gene tables. It therefore cannot construct a gene ref, so the fact that
  `sqlRefFromRow` reads `value_type` optionally (verified: `sqlref_scan.go:292-296` only
  populates it when the SELECT carries the column) is harmless: no production query scans a
  gene ref.
- Transfer (M6) reaches `ResolveSQLValueRef` only via `CollectSettingsValue`/`SetFromCollected`,
  which the production surface (`transfer_bridge.go`) never wires — it wires only
  `CollectEntry`→`AppendEntry` (whole-entity JSON grafts that never resolve a gene cell ref).
  Confirmed `transfer.go` does not build a gene cell ref.

The executor's claim ("production paths unaffected; the scan reads value_type optionally")
is verified end-to-end. The guard cannot reject a legitimate production write.

## 3. Scene version gate — PASS

Non-fatal `SeverityWarning` `scene_version_unsupported`, emitted only when version is
non-empty AND not in `knownSceneVersions`; parsing continues unchanged. Style matches sibling
diagnostics (`scene_not_object`, `unknown_json_section`). `knownSceneVersions` is seeded
`{"0.6.3","0.6.3.1"}` — and BOTH appear in the real `.zip` save fixtures
(`autosave_20260228004041.zip`→0.6.3, `dasdasd.zip`→0.6.3.1), so the suite's real saves stay
quiet (no spurious count-zero failures). Tests cover known/bogus/no-version and confirm the
diagnostic reaches the normalized diagnostics table while still parsing `nBibites`/`simulatedTime`.

## 4. Scope — PASS

Only the intended product touches; deferred files (`session.go` cascades, `path.go`,
`parse_payload.go`, `transfer*.go`) untouched.

## Tests run

`go test -count=1 ./savemutator/thebibites/... ./script/thebibites/... ./saveparser/thebibites/... ./duckdb/...`
→ all green (savemutator 14.2s, script 59.9s, saveparser/tests 49.6s, duckdb 42.6s).
No `-race` (purely sequential: a map lookup + a string compare per write/scene; no
goroutine, mutex, or `t.Parallel` added). No perf lens (O(1) per write/per scene parse; no
loop, N+1, or unbounded scan; plan carries no Perf-review flag).

VERDICT: PASS
