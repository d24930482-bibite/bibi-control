# M4 Review — Unified child-collection surface (+ remove `gene()`)

Reviewer: independent. Scope: read-only review of `main...HEAD` on worktree
`/home/asemones/Documents/personal_code/bibicontrol-wt/M4`. Judged against the source
spec `docs/missing.md` §4 and the plan `docs/tickets/M4.plan.md`. This is the DSL-chain
ROOT (fan-out 4: M1/M5/M7 direct, M2 transitive) — reviewed the contract for exactness.

## Spec/plan fidelity

Plan faithfully covers §4's M4-slice; the deferrals (write-idiom → M7, `.where`/spanning →
M1, brain-nav → M5, cheatlist generation → M2, zones reconcile → M7) each name an owner and
match the spec's own decomposition. No dropped/narrowed/contradicted M4 requirement found.
Implementation matches the plan.

## Verification of the herder's checklist

1. **`gene("X")` fully removed.** Confirmed all four cut points:
   - `entity.go` `Attr`: the `case "gene":` arm is gone.
   - `entity.go` `AttrNames`: `"gene"` dropped from the appended list; `"genes"` kept.
   - `entity.go` `SetField`: `gene` removed from the read-only guard token (falls through
     to "unknown attribute" now).
   - `entity.go` `geneBuiltin`: impl deleted entirely.
   Grep across the package: no surviving `geneBuiltin`, no `.gene(` call site, no third
   caller. Remaining `"gene"` literals are the regression-guard test and `Gene.Type()`
   (the type-name string, unrelated). The regression guard
   (`gene_test.go:75`) asserts `Entity.Attr("gene")` returns `(nil, nil)` — proves it's gone.

2. **`.get` semantics exact (mutation-checked by the tests).**
   - `GeneCollection.getBuiltin` reuses `c.Get` → returns the value-direct scalar on hit
     (matches the removed `gene()`), `dflt` (init'd to `starlark.None`) on `!found`,
     and PROPAGATES the foldLookup collision `err` (does not swallow to default).
     `TestGeneGetCollisionLoud` asserts the collision is loud even WITH an explicit default
     supplied — the strongest form of this check.
   - `SettingScope.getBuiltin` mirrors it exactly, returning the `*Setting` HANDLE on hit.
     `TestSettingScopeGet` asserts handle type, recased hit, None/supplied-default on miss,
     and loud collision (with and without explicit default).
   - `UnpackArgs(..., "key", &key, "default?", &dflt)` with `dflt = starlark.None` —
     Starlark-dict-exact signature on both surfaces.

3. **Subscript `["X"]` stays loud on BOTH surfaces, asymmetry asserted both directions.**
   - Genes: `gene_test.go:70` asserts `Get(absent)` → `(nil,false,nil)` (Starlark
     KeyError); `bindings_test.go` `TestGeneRead` gained a sibling asserting `.get` → None
     vs subscript loud.
   - Settings: `TestSettingScopeGet` asserts `sc.Get(absent)` → `(nil,false,nil)` alongside
     `.get` → None.

4. **`SettingScope` iterable + `len()` in deterministic name-sorted order.** The new
   `loadedsave.go:settingRows` enumerates `byName` keys and `sort.Strings(names)` BEFORE
   building the row slice — ordering is ENFORCED, not incidental. `Len()`/`Iterate()` both
   route through it; iteration yields `*Setting` handles. `TestSettingScopeIterate` asserts
   the handle type, the count, AND name-sorted order (`h.row.SettingName < prevName` guard)
   plus the empty-scope path — catches the silent Go-map-order flake risk.

5. **Write paths untouched.** No diff in `subcollection.go`/`zone.go`; no added/modified
   `SetKey`/`setBuiltin`/`setSettingValue`/structural-`append`/`delete` in the script diff.
   M4 added read/lookup surface only. M7's write convergence is unobstructed.

6. **app.js hand-matched, deferred items absent.** Flat pool: `'gene'` → `'get'`
   (`'genes'` already present). Added `setting_scope: ['get']` member set + wired
   `AC_RESULT.settings.{simulation,independent,material} → setting_scope` so `.get`
   completes. No generation logic (M2's mandate). Deferred §4 items (write convergence,
   `.where`/spanning, brain-nav, cheatlist) not implemented — diff leak-grep clean.

## Extra checks

- `settingRows` ownership resolution (case-folded material owner, loud ambiguous-owner
  error, absent→nil) faithfully mirrors the existing `settingRow`. Row pointers from `Get`
  and from iteration both point into the same `settingsBacking` slice at the same index, so
  write-through consistency holds between subscript and iteration handles.
- `Setting{ls, table, row}` shape is identical across `Get` and the iterator; `row` field
  type (`*tb.SettingValueRow`) matches `settingRows`' return element.
- Egg parity: removal/`.get` go through the shared `Entity` type via `geneTable(kind)`; no
  egg-specific point-read code remains (grep confirmed).

## Tests run (shared cache)

- `go vet ./script/thebibites/` — clean.
- `go test -count=1 -v -run '<M4 contract tests>' ./script/thebibites/` — all 9 targeted
  tests RUN (none skipped) and PASS, including both collision-loud and iteration-order tests.
- `go test -timeout 20m ./...` — full suite green (script pkg fresh; rest cached/byte-id).
- `-race` intentionally skipped: change is purely sequential (read surface + map `.get` +
  deterministic sort), no new goroutines / shared mutable state / `t.Parallel`. No perf
  surface (plan carries no Perf-review; in-memory index reads, nothing new per-row).

## Verdict

Contract is exact: the loud/tolerant asymmetry holds both directions on both name-keyed
surfaces, collisions propagate loud through `.get`, settings iteration is deterministically
ordered, `gene()` is fully excised with a regression guard, and write paths are untouched.
The four dependents can build on this stably.

VERDICT: PASS
