# M1 review — Spanning surface: genes/brains kinds + time axis + world_id wrapper

Reviewer: independent. Scope: read-only review, no product/test edits. Diff reviewed:
`main...HEAD` on worktree `bibicontrol-wt/M1`.

## Verification summary

Spec source `docs/missing.md §1` read and cross-checked against `M1.plan.md` and the
implementation. The plan faithfully covers §1 (genes/brains as spanning kinds, time axis
via existing sim_time grouping, all-worlds `world_id` CTE wrapper, read-only by
construction) with the deferred items explicitly carved out. The implementation matches
the plan. No drift found.

### 1. Cross-world isolation invariant (the silent-leak bug) — PASS

- The UNION-ALL identity for each spanning kind carries `save_id` first in BOTH members
  (`spanningProjection` puts `save_id` at index 0; `spanningFromExpr` emits the same
  column list for every carrier). The scope clause
  (`<alias>.save_id IN (SELECT save_id FROM mirror_saves [WHERE world_id = ?])`,
  scope.go:90-98) binds to `"genes"`/`"nodes"`/`"synapses"`.alias.save_id.
- **Mutation check performed:** I neutered `spanningScope.scopeClause` to `"1 = 1"`
  (dropping the save_id partition) and ran `spanning_kinds_isolation`. It FAILED loudly:
  per-world counts collapsed to the all-worlds total (genes A=127332 / B=127332 vs
  expected 40405 / 23261), and even the workspace assertion went red (127332 vs A+B
  63666). The test asserts exact per-world counts (not non-zero) at all three points, so
  a dropped scope clause cannot pass. Restored the file; worktree clean.
- `world.genes.mean()` returns the single-world value (`NewWorldHistoryScope`), and
  `workspace.genes.mean()` the all-world value (`NewWorkspaceScope`) — confirmed by the
  passing isolation + group_by + where tests.

### 2. Working-path byte-identity — PASS

- `loadedsave.go` (containing `buildAccess`) is NOT in the diff — untouched.
- The `ls.access["bibite_genes"] == nil` tripwire in `TestSpanningKindRegistration` is
  present and real; it (plus the egg/brain table variants) passes, proving no spanning
  table leaked into the single-save working path. The spanning kinds are registered in a
  separate `spanningEntityTables` map and resolved via `tablesForKind` fallback;
  `buildAccess` still iterates `entityTables` alone.
- The single-save attr registry still exposes the working kinds (e.g. `bibite.energy`),
  asserted additively.

### 3. UNION-ALL identity carries save_id through both members — PASS

- `TestSpanningFromExprShape` locks the derived FROM: both `bibite_*` and `egg_*` members
  present, `save_id` projected first, aliased to the plural kind name. Verified the
  projected source columns for each kind (gene: save_id/gene_name/value_type/number_value;
  synapse: save_id/innovation/node_in/node_out/weight; node: 11 cols incl save_id). Every
  friendly column is qualified against the union alias (`@genes`/`@nodes`/`@synapses`), so
  `resolveColumn` emits `"<alias>"."<col>"` which binds to the union projection and the
  catalog LEFT JOIN.
- `TestSpanningAggregateOverFixture` proves the compiled aggregate equals the direct
  `count(*)` ground truth over both carriers scoped to the save.

### 4. workspace.nodes NOT shadowed — PASS (asymmetry acceptable, well-documented)

- `workspaceValue.Attr` spanning case is `"bibites","eggs","pellets","genes","synapses"`
  — `nodes` deliberately excluded. `case "nodes":` (automation.go:103) still returns
  `nodesBuiltin` (the host-trusted cluster process-node listing) unchanged. No regression.
- `worldValue.Attr` spanning case correctly includes `"nodes"` (world has no process-node
  builtin), so `world.nodes` is the spanning brain-node aggregate. The
  world-has-3 / workspace-has-2 asymmetry is intentional, documented inline, and the
  all-worlds brain-node aggregate remains reachable via `world.nodes`. AC_TYPE_MEMBERS /
  AC_RESULT mirror this exactly (workspace nodes stays type `node`; genes/synapses are
  `collection`). Acceptable surface, not a wart.

### 5. Read-only / aggregate-only / world_saves wrapper — PASS

- Mutation through spanning kinds rejected: `set/set_expr/delete` are hidden on read-only
  scopes (collection.go:160-165); `EntryNames()` rejects spanning kinds at the `readOnly()`
  guard and any non-bibite/egg at the default case (no gene/node/synapse can reach
  transfer). `spanning_kinds_mutation_rejected` passes for genes.set / synapses.delete /
  nodes.set.
- All-worlds `Query` gets the prepend-only `world_saves` CTE
  (`WITH world_saves AS (SELECT save_id, world_id, sim_time FROM mirror_saves) <query>`),
  mirroring the existing HistoryQuery precedent, applied AFTER `ensureReadOnly`.
  `TestQueryWorldSavesWrapper` proves a raw all-worlds query can `JOIN world_saves
  USING(save_id)` with no hand-written mirror_saves JOIN, and that a `DELETE` through the
  wrapped path is still rejected. The unconditional prepend (a user `WITH …` query would
  become invalid) is identical to the pre-existing HistoryQuery behavior, not a new
  regression — the plan specifies mirroring that shape.

### 6. Deferred items NOT implemented — PASS

- No `bucket()` terminal, no `group_by("world")` alias (only `world_id`), no
  `ensureReadOnly` parser rewrite (only a clarifying comment added — no logic change to
  forbiddenVerbs / nextToken / return paths), no workspace-wide gene/brain mutation, no
  node↔synapse graph join. All confirmed absent in the diff.

## Perf lens

Plan carries `Perf-review: none` and the diff adds no per-row / N+1 / per-revision loop —
a gene `.count()` is the same single SELECT as a bibite `.count()`, over a UNION-ALL
identity. The catalog refresh and one-aggregate-SELECT-per-call shape are inherited
verbatim. No perf concern. (`-race` correctly not run: the diff adds no goroutines, shared
mutable state, or `t.Parallel` — purely a SQL-builder/registry/binding change.)

## Tests

`go test ./script/thebibites/... ./workspace/...` passes (fresh `-count=1`). Targeted key
tests run verbosely all green: TestSpanningTwoWorlds (incl. spanning_kinds_isolation /
group_by_world_id / where_no_join / starlark / mutation_rejected),
TestSpanningKindRegistration, TestSpanningFromExprShape, TestSpanningAggregateOverFixture,
TestQueryWorldSavesWrapper, TestSpanningCollectionAttrHidesMutation,
TestSpanningReaderConstruction. The cross-world isolation tests assert exact per-world
counts (verified leak-detecting via the mutation check above), not non-zero. Tests are
real and meaningful, not weakened to pass.

VERDICT: PASS
