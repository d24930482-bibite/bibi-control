# F3 re-review — cross-world species REMAP on whole-entry graft (post-bounce)

## Outcome
The bounce fix is real and sufficient, and the rest of the ticket re-confirms. PASS.

## 1. The fix is real (mutation-verified)
The prior FAIL: the allocator's live-dest-entity species-id source in `dstSpeciesIDUsage`
survived deletion with a green suite — the exact F1 silent-conflation seam, untested.

Commit `9c5fcc6` adds `TestTransferAppendEntryAllocatorBeatsLiveEntitySpeciesID`
(transfer_test.go:706). Its fixture makes the live-entity scan the SOLE source of the
global max: nextSpeciesID set stale to 1; a record-only id 50 appended to
recordedSpecies (recordedSpecies now {1,2,3,50}); activeSpeciesList stays [1,2,3]; a LIVE
dest bibite (bibite_0) retagged to speciesID 60. 60 appears in NEITHER activeSpeciesList
NOR recordedSpecies, and nextSpeciesID is stale — so only the live-entity traversal can
surface it. The test asserts the grafted fresh id `> 60`.

I ran the mutation myself:
- Baseline: test PASS; fresh id = 61.
- Removed the live-entity loop (transfer_identity.go:118-130): the test FAILS with
  "grafted genes.speciesID = 51, want > 60 ... silent-conflation seam", and the whole
  `-run 'Transfer|Species'` suite goes RED (it was green under this same mutation before
  the fix). 
- Restored: clean (no diff), full package fresh run green (42s).
The conflation invariant is now mutation-pinned. Fix sufficient.

## 2. No product-code drift in the fix
`git diff 037993d..9c5fcc6` over transfer_identity.go / transfer.go / append_delete_test.go
is EMPTY. `9c5fcc6 --stat` shows transfer_test.go +50, nothing else. The bounce is
test-only; the allocator/remap logic that passed before is byte-identical.

## 3. Rest of the ticket re-confirmed (not assumed)
- Allocator beats max of ALL FOUR sources: activeSpeciesList, recordedSpecies[*].speciesID,
  every live dest bibite/egg genes.speciesID, and nextSpeciesID; fresh = max(maxUsed+1,
  nextSpeciesID) (transfer_identity.go:74-132). Stale-nextSpeciesID case pinned
  (TestTransferAppendEntryAllocatorBeatsStaleNextSpeciesID), record-only-id case pinned,
  live-entity case now pinned.
- Headline colliding-linear-id (TestTransferAppendEntryRemapsCollidingSpecies): succeeds;
  asserts grafted id != 2 and not in [1,2,3]; imported record carries SOURCE marker 22.0
  (not dest 2.2); dest species-2 record marker untouched (2.2); dest members bibite_1/_2
  still speciesID 2 (no conflation); imported parentID reset to self.
- genes.speciesID rewritten on bibite (headline/absent) AND egg
  (TestTransferAppendEntryRemapsEggSpecies, egg_1 gets fresh id, source-8 marker imported).
- Record import: deep-copied (sourceSpeciesRecord returns cloneJSON), speciesID stamped to
  fresh, parentID reset to self (asserted), nextSpeciesID bumped to fresh+1 only when
  present, recordedSpecies + activeSpeciesList appended; all staged through the dst Session
  before the entry append (transfer.go:232-255) so a rejected graft leaves 0 staged ops.
- Loud guards kept: body.id collision (reconcileGraftIdentity), dangling child ref
  (TestTransferAppendEntryDanglingChildRef — 0 staged ops), source-record-absent
  (TestTransferAppendEntrySourceRecordAbsentLoud — names id 7, unstaged), no-dest-table
  (TestTransferAppendEntryNoDestSpeciesTableLoud — names "species table", 0 staged ops).
- Species-less grafts cleanly, table untouched (TestTransferAppendEntrySpeciesLessGraftsCleanly).
- Scope fence: transfer.go / transfer_identity.go import only saveparser/thebibites (tb)
  plus stdlib. No DB/IPC/head/DSL.
- Spec faithfulness (workspace_plan.md#f1, "phase it, fail loudly on unhandled cases"):
  plan and implementation cover species-id reconcile + record merge, keep body.id/parent/
  child-ref loud, and fail loud on unhandled cases. Optional exact-match dedup deferred with
  a documented pointer (safe: duplicating a record is safe, conflation is not).

## 4. Suite
Forced fresh `go test ./savemutator/thebibites/ -count=1`: ok (42.1s). Full `go test ./...`:
all packages ok.

VERDICT: PASS
