# D1 Review (re-review after FAIL) — node lifecycle: start/persist/bind + active set

Reviewer: independent (opus). Worktree:
`/home/asemones/Documents/personal_code/bibicontrol-wt/D1`. Base: `main`.

This is a re-review after a prior FAIL whose blocking item was: `StartNode` did not
re-check the one-node-per-world invariant after re-acquiring `w.mu` post-`Start`, so two
concurrent starts for one world could both bind it. Plus two clarity nits (a dead no-op
loop and a misleading "running rows" comment).

## What was checked

- Diff vs `main...HEAD`: `workspace/node.go` (315), `workspace/node_test.go` (415), and
  the single `Close()` drain edit in `workspace/workspace.go`. `git status --porcelain`
  shows only `.current-ticket` (the reviewer marker). Scope is method-additive and
  merge-clean with C1 as the plan requires; no `Workspace` struct field added.
- Spec fidelity: `docs/workspace_plan.md` §D1 (L356-360) and the locked Core-model line
  "A world is bound to at most one active node at a time" (L78-79). Plan faithfully
  covers the spec; implementation matches the plan.
- Suite re-run myself (uncached, `-count=1`): `go test ./...` green. `go test -race
  -count=1 ./workspace/` green (incl. the new concurrency test). `go vet ./workspace/`
  clean; `gofmt -l` on the three files clean.

## Prior-FAIL items — all resolved

1. **One-node-per-world re-check after the dropped lock (the blocking item) — FIXED and
   correct.** The second critical section (`node.go` ~L121-166) now re-runs the FULL
   per-world scan via `activeNodeForWorldLocked` (L131-142), not just the dup-id check,
   before `CreateNode`. On conflict (or a re-check error) it `Unlock`s and kills the
   orphan (`rt.Kill()` + `rt.Close()`) before returning. Crucially, `CreateNode` (persist)
   AND `w.nodes[nodeID] = rt` (active-set insert) both execute inside the SAME `w.mu` hold
   as the re-check, so there is no observable window where a winner is persisted-but-absent
   from `w.nodes` (or vice versa) that a losing goroutine could slip through. The re-check
   maps node_id->world_id from `ListNodes` and intersects with live `w.nodes` membership,
   so a stale persisted row never blocks a fresh bind (liveness anchored on the active set).

2. **The new concurrency test (`TestStartNode_ConcurrentSameWorld`) is genuine, not
   weakened.** It launches 8 goroutines starting distinct logical ids for one world and
   fatals unless exactly 1 wins and `n-1` fail, then asserts the active set holds exactly
   that one winner. I confirmed it actually guards the invariant by an independent
   regression experiment: I copied the worktree to a scratch module, deleted ONLY the
   second-critical-section per-world re-check, and ran the test under `-race` — it failed
   with "8 winners, want exactly 1 (failures=0)". So all 8 goroutines genuinely reach the
   post-`Start` re-check region concurrently, and the test catches the exact prior bug.

3. **`-race`:** `go test -race -count=1 ./workspace/` passes for all D1 tests including
   `TestStartNode_ConcurrentSameWorld`. Note: a full `go test -race ./...` shows an
   unrelated failure in `script/thebibites` (a DuckDB `database/sql` connection-opener
   goroutine flake under the race detector); that package is untouched by D1, and it
   passes cleanly without `-race` (`ok ... 156s`). The gate's standard suite (`go test
   ./...`) is fully green. Not attributable to this ticket.

4. **Two clarity nits — FIXED.** The dead `for activeID, _ := range w.nodes { _ = activeID }`
   loop is gone; the first critical section calls `activeNodeForWorldLocked` directly. The
   misleading comment now reads "across ALL persisted rows (no status filter; liveness is
   decided by w.nodes membership below)", matching the code.

## Other seams (re-confirmed correct)

- node_id-vs-PK indirection: `setNodeStatusByLogicalID` resolves the nodes PK from the
  logical id via `ListNodes` and passes it to `SetNodeStatus` (which keys on PK). Stop/Kill
  hit the right row.
- Orphan-process cleanup: every post-`Start` failure path (concurrent-dup, world re-check
  error, world re-check conflict, `CreateNode` error) kills+closes the runtime before
  returning. `Runtime.Kill()` kills the OS process.
- `Close()` drain: kills+closes each active runtime, joins errors, clears the map, then
  closes DB/registry/blob handles; nil- and empty-map-safe, consistent with the B2 (no
  lock) convention.

VERDICT: PASS
