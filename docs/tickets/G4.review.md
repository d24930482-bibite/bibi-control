# G4 Review — Guardrails: ErrNotRematerializable on blob-dependent paths

## Scope verified
- Diff touches exactly six files: `workspace/errors.go` (new), `workspace/errors_test.go` (new),
  `workspace/working_set.go`, `workspace/node_reload.go`, `workspace/eviction_test.go`,
  `workspace/node_reload_test.go`. No product code outside the two pre-marked guard sites changed.

## Spec / plan fidelity
- Source spec `docs/workspace_plan.md#g4` requires: every blob-byte path checks `blob_present`,
  returns a typed `ErrNotRematerializable` (errors.Is-matchable, surfaced cleanly by E1), no
  mirror→blob fallback. The plan's audit faithfully covers this and the implementation matches.
- The spec bullet names the F1 transfer source read among blob_present checkers; the plan's audit
  (items 3-4) correctly resolves this: transfer.go operates on already-opened `*Session` objects
  and has NO direct blob read (`grep blobs()/revisionstore/.Get(` in transfer.go returns nothing);
  the read that produced those Sessions is in `Load`/`OpenWorld`, now guarded. Not a drop — the
  guard is upstream, exactly as the spec's "every op needing bytes" requires.

## 1. Unbroken %w chain — PASS
- `Load` (working_set.go:62-64) raises the sentinel directly via `notRematerializable(...)`,
  which wraps `ErrNotRematerializable` with `%w`.
- `OpenWorld` (working_set.go:161) returns `w.Load(...)` directly — identity preserved.
- `CommitWorld` (commit.go:53-56) returns `OpenWorld`'s error with bare `return ..., err` — preserved.
- `ReloadNode` (node_reload.go:77-80) wraps `headBlobBytes` with `%w`; `headBlobBytes`
  (node_reload.go:123-124) raises the sentinel directly. Chain intact to the public boundary.
- No `%v` error wrap exists on working_set.go / node_reload.go / commit.go (grep clean).

## 2. Exactly two guard sites, no over-guard — PASS
- Only `working_set.go` Load and `node_reload.go` headBlobBytes changed.
- `commit.go` reparseCommitted (blobs().Get at line 167) NOT touched — correct (same-tx full blob).
- F1 savemutator/thebibites/transfer.go NOT touched and has no direct blob read — correct.
- Full grep of `blobs().Get` / `BlobPresent` across workspace/, savemutator/, revisionstore/
  surfaces no other un-guarded blob-byte consumer in product code. No missed site.

## 3. Tests are real errors.Is — PASS
- `eviction_test.go` TestEvictThenLoadIsNotRematerializable: tightened to
  `errors.Is(err, ErrNotRematerializable)`; uses a genuine `EvictRevisionBlob` (bytes actually
  deleted, Get + Has confirmed gone) — the full-eviction proof.
- `node_reload_test.go` TestReloadNode_MirrorOnlyHeadRefused: tightened to errors.Is; keeps the
  "no drop file written / no RELOAD sent" ordering assertions.
- NEW TestLoad_MirrorOnlyHeadRefused and TestOpenWorld_MirrorOnlyHeadRefused: both assert
  errors.Is(...ErrNotRematerializable). OpenWorld test Unloads first to force the slow path,
  proving Load delegation propagates the sentinel.
- `forceHeadMirrorOnly` does a flag-only flip (`UPDATE ... tier='mirror_only', blob_present=0`),
  the exact G2 demotion shape; the guard fires on blob_present=0 before any blobstore Get, so it
  is genuinely exercised. `rev.ID` is int64, matching the helper signature and `%d` format.

## 4. Suite + sentinel placement — PASS
- `go test ./...` green (fresh, no failures).
- Four guard tests re-run fresh (`-count=1 -v`): all PASS (Load 5.0s, OpenWorld 4.5s,
  EvictThenLoad 18.1s, ReloadNode 4.3s) — not cached, not skipped.
- Sentinel `ErrNotRematerializable` lives in `workspace/errors.go`, one-sentinel-per-concern style,
  exported with the exact name/package E1 will errors.Is against.

## Perf lens
Skipped per plan (no Perf-review flag): guards run once per Load/Reload, add no round-trips.

VERDICT: PASS
