# B2 Review

## Summary

Ticket B2 implements the `workspace/` package skeleton as specified. Two files were added:
- `/home/asemones/Documents/personal_code/bibicontrol-wt/B2/workspace/workspace.go`
- `/home/asemones/Documents/personal_code/bibicontrol-wt/B2/workspace/workspace_test.go`

No other files were modified, which is correct per the plan's "No other files" constraint.

## Spec/Plan Alignment

The spec (`docs/workspace_plan.md#b2`, lines 284-293) requires:
- `Workspace` type holding `owner`, `id`, `*revisionstore.Store`, `blobstore.Store`, per-workspace `*sql.DB`, `map[worldID]*thebibites.LoadedSave`, `map[nodeID]*noderuntime.Runtime`, and `sync.Mutex`. All present and correctly typed.
- `Open`/`Create(root, owner, name)` creating/looking up the `workspaces` row and opening `<root>/workspaces/<id>/analytics.duckdb` via `duckdb.Open` + `duckdb.ApplyMigrations`, plus shared registry + blobstore. Implemented correctly.
- Layout: `metadata.sqlite` shared, `blobs/` shared, `workspaces/<id>/analytics.duckdb` per-workspace. Path helpers encode exactly this.

The plan faithfully covers the spec. The implementation matches the plan.

## Implementation Details Verified

**Struct:** All required fields present. `blobsStore` held as `*blobstore.FSStore` (concrete, not interface) so `Close()` can reach `FSStore.Close` which the `blobstore.Store` interface does not expose. The `blobs()` accessor returns `blobstore.Store` interface. This matches the plan's discussion of the concrete-vs-interface tension.

**Create:** Order is correct (root mkdir, open registry, open blobstore, create registry row, mkdir workspace dir, open+migrate DuckDB, assemble). Partial failure cleanup closes opened handles in reverse order (no leaks). No files/rows deleted on failure (correct for v1).

**Open:** Validates both `root` and `id`. Opens registry first, calls `GetWorkspace`, closes registry on not-found before returning. `revisionstore.IsNotFound` is correctly threaded through the wrapping chain so the test's `revisionstore.IsNotFound(err)` check works. Opens blobstore then DuckDB with same cleanup discipline.

**Close:** Sets niled handles after closing, making it truly idempotent. `errors.Join` collects all errors.

**`openDuck`:** Extracted as a private helper — clean factoring, called by both Create and Open.

**Mutex:** Declared but not locked in B2 (correct). Doc comment names the future lock holders (C3/D3).

**Working-set maps:** Allocated with `make` in both constructors, never nil after construction.

**No import cycles:** `go build ./...` is clean.

## Tests Verified

All 5 required tests are present and meaningful (run with `-count=1` to bypass cache):
- `TestCreateWorkspaceEstablishesLayout`: Checks ID/Owner/Name, layout on disk, DuckDB usability, migrations (both `information_schema.tables` count > 0 and `SELECT count(*) FROM bibites == 0`), empty working-set maps.
- `TestCreateThenOpenRoundTrips`: Closes created handle, reopens by id, verifies all fields round-trip through the registry.
- `TestOpenUnknownWorkspace`: Verifies nil workspace returned, non-nil error, and error wraps `IsNotFound`.
- `TestSharedRegistryAndBlobsAcrossWorkspaces`: Two workspaces under one root get distinct IDs and DuckDB paths; single `metadata.sqlite` and `blobs/` dir; `ListWorkspaces` confirms both rows in the shared registry.
- `TestCloseIsIdempotentAndNilSafe`: First Close returns nil; second Close does not panic and returns nil; `(*Workspace)(nil).Close()` returns nil.

All tests passed. `go vet ./workspace/...` clean.

## No Issues Found

VERDICT: PASS
