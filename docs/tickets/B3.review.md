# B3 Review — Remove the `save` global; `open()` returns a `Save`; rewrite tests

## Scope verified against the plan

The diff is exactly the global→object refactor + test rewrites the plan calls for,
confined to `script/thebibites/` (12 files: 2 production, 10 tests). No opportunistic
edits to mirror/sql/zones logic — barrier discipline respected.

## Production changes

- `bindings.go`: `Globals` swaps `"save": &Save{ls: ls}` → `"open":
  starlark.NewBuiltin("open", openBuiltin(ls))`. `autocommit` + `hostAggregates`
  unchanged. New `openBuiltin(ls)` mirrors the `autocommitBuiltin` closure shape,
  captures `ls`, accepts an optional `path?` (UnpackArgs), ignores it, and returns
  `&Save{ls: ls}`. Doc comment updated to say it provides `open()`, not `save`.
- `save_value.go`: doc-comment tightening only (no logic change). `String()`/`Type()`
  still return `"save"` — left as-is, which the plan explicitly permits (cosmetic,
  no test asserts on it; confirmed by grep).
- `run.go`: not in the diff. Still calls `Globals(ls)` at run.go:60. Because
  `openBuiltin` captures the same host-preloaded `ls`, program-obtained Saves share
  the one `LoadedSave`, so staged mutations reach `prepareCommit`. This is the subtle
  commit seam the plan flagged; it is handled correctly.

## Seam checks

- **open() binds the preloaded ls, not a fresh load.** Confirmed: no re-`Load` /
  re-`ExtractTables` in `openBuiltin`. `TestRunAndCommitChurn` (one WriteArchive,
  zero reparses) and `TestRunAndCommitVerify` PASS, proving the commit/churn path is
  byte-for-byte unchanged by the move.
- **Standalone globals not prefixed.** `autocommit(False)`, `host = median([...])`
  remain unprefixed in the rewritten programs. Verified in commit_test.go (opt-out)
  and analytics_test.go.
- **Go-level Save constructions preserved.** `save := &Save{ls: ls}`
  (mutation_test.go:575) and `s := &Save{ls: ls}` (analytics_test.go:73,125) are
  direct Go and left untouched — still valid.
- **Negative-path diagnostics still real.** `TestScriptMissingAttrDiagnostic`
  (bad attr → diagnostic, no panic) and `TestBadPredicateIterationDiagnostic`
  (typo'd `where` → loud "panic" diagnostic, asserts err != nil AND one diagnostic
  with the expected code) genuinely assert errors — not weakened to pass. They prove
  the object surface diagnoses identically to the former global.

## Straggler sweep

- `grep -rnE 'save\.(bibites|eggs|sql|settings|zones|pellets|commit)'` over test
  files returns only Go comment lines — zero in any Starlark program string.
- `grep -rn '"save"'` returns only the cosmetic `String()`/`Type()` returns in
  save_value.go; the `Globals` key `"save"` is gone.
- No external consumer of `thebibites.Globals` / `thebibites.Save` outside the
  package (grep clean).

## Build / vet / tests (re-run, fresh cache where it matters)

- `go build ./...` — exit 0
- `go vet ./script/thebibites/` — exit 0
- `go test -count=1` of all program-driven + negative + churn/verify cases — all PASS
  (TestMutationViaScript, all TestRunAndCommit*, TestAnalyticsViaScript,
  TestSettingsViaScript, TestPendingZoneValueViaScript, TestSubCollectionAppendViaScript,
  TestDeleteViaScript, TestGeneWriteViaScript, TestGeneCollectionViaScript,
  TestScriptRunReads, TestScriptMissingAttrDiagnostic, TestBadPredicateIterationDiagnostic).
- `go test ./...` (full repo) — all packages ok; nothing else referenced the global.

The cutover is complete, correct, and tested; no back-compat shim; barrier-clean.

VERDICT: PASS
