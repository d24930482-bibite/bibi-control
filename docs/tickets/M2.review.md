# M2 Review — Generated cheatlist / quick-reference page

Reviewer: independent. No product code changed during review.

## Scope checked
Source spec `docs/missing.md` §2 (incl. "Caveat — the root is deeper"), the plan
`docs/tickets/M2.plan.md`, and the diff `main...HEAD` (62f0e0d).

## Spec ↔ plan ↔ implementation
- §2 "Target" bullets all delivered: type→members graph (one row/type), read/write/iterate
  distinctions, spanning-vs-single-world, and 7 copy-paste recipes (covers the 6 required:
  open+iterate, where().set()+commit, group_by, query table, start_node, transfer).
- Intentional, documented divergence from the literal spec text: spec shows `gene("X")` parens
  → None, but M4 (merged dep) removed `gene("X")`. The plan mandates documenting the surface
  AS MERGED; the page explicitly states "gene("X") was removed in M4" and documents the
  `genes.get("X")` tolerant vs `genes["X"]` loud split. Correct, not drift.
- Caveat handled per plan: AC_TYPE_MEMBERS anchored to AttrNames() via a Go cross-check; the
  highlighter second-vocab fold and data-derived column lists are explicitly deferred and the
  page documents data-derived surfaces by SHAPE + live dir() (honors save-format-churn memo).

## Generated-not-hand-maintained (load-bearing)
- Member graph flows from live AttrNames(): script/thebibites/cheatlist.go and
  workspace/cheatlist.go call AttrNames() on instances (no re-typed strings). The two
  EntityCollection branches are constructed correctly: NewWorkspaceScope() → read-only
  (spanningScope.writable()==false), nil scope → mutable. Verified against collection.go,
  scope.go, save_value.go, settings_value.go, gene.go, automation.go.
- `go generate ./saveparser/thebibites` produces a byte-identical cheatlist.html (committed
  file in sync) and is idempotent across two consecutive runs.

## Cross-check test has teeth (verified by mutation, then restored)
- Arm 1 (cheatlist.html ↔ bindings): dropping a member from a generated row → RED.
- Arm 2 (AC_TYPE_MEMBERS ↔ bindings): re-adding phantom settings.zones → RED with the exact
  expected message (the case the plan called out).
- Arm 3 (SL_METHODS coverage): exemption list audited against the full canonical member union
  — every exemption is a genuine non-method scalar attr or top-level builtin; no real drift is
  masked and no false pass is hidden. A few exemptions are harmless no-ops (name already in
  SL_METHODS).

## Other
- Drift-fix (i) applied: phantom zones removed from AC_TYPE_MEMBERS.settings; collection
  annotated as mutable branch; both branches documented in prose.
- M5 conditional nav attrs correct (source/target synapse-only, inputs/outputs node-only) per
  subcollection.go:238; workspace.nodes asymmetry documented.
- web_test.go extended to serve /cheatlist.html and assert index links it.
- go build + go vet clean on script/thebibites, workspace, api, cmd/gen_dsl_cheatlist.
- No concurrency in the diff (-race not warranted); no perf surface (generation runs once over
  ~10 static slices — Perf-review: none is correct). No stray build artifacts in the worktree.

VERDICT: PASS
