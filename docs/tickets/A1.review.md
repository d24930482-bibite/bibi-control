# A1 Review — Registry schema: worlds/nodes + world_id on revisions

## Scope checked
- Diff against `main...HEAD`: `go.mod`, `revisionstore/schema.sql`, `revisionstore/store_test.go` only.
- `store.go` / `RevisionInput` untouched — A2's territory left intact (verified no `WorldID`/`Tier`/
  `Refcount`/`BlobPresent` fields leaked into `store.go`). Good scope discipline.

## Schema (schema.sql)
- `workspaces`, `worlds`, `nodes` added as `CREATE TABLE IF NOT EXISTS` with the exact DDL from the
  plan (PKs, FK `ON DELETE RESTRICT` / `SET NULL` per plan).
- Five new `save_revisions` columns added **inline** in the `CREATE TABLE IF NOT EXISTS` body
  (no `ALTER TABLE`) — matches the plan's idempotency requirement exactly; defaults and CHECK
  constraints (`tier`, `blob_present`, `refcount`) match the plan verbatim.
- Indexes added: `worlds_workspace_id_idx`, `nodes_workspace_id_idx`, `nodes_world_id_idx`,
  `save_revisions_world_id_idx`. Existing `save_revisions(sha256)` index not duplicated.
- File ordering (`script_runs` -> `workspaces` -> `worlds` -> `save_revisions` -> `nodes`) is the
  readable order the plan asked for; the mutual `worlds.head_revision_id` <-> `save_revisions.world_id`
  FK pair is declarable since SQLite resolves FK targets at write time. No `mirror_saves` table and no
  `blob_refs` table — correctly omitted per plan.

## go.mod
- `github.com/google/uuid v1.6.0` promoted from indirect to direct require block. `go.sum` unchanged,
  which is correct: uuid was already an indirect dep so its hashes were already present. `go build ./...`
  succeeds.

## Test (TestSchemaAppliesAndIsIdempotent)
- Real and meaningful, not weakened:
  - Re-runs `ApplyMigrations` on the live `store.db` and asserts no error — genuinely guards the
    documented `ALTER TABLE` trap (a second full-schema `Exec` would fail if columns were added via
    ALTER).
  - Asserts all three new tables, all five new columns, and all four new indexes exist.
  - Asserts `tier`/`blob_present`/`refcount` defaults from `PRAGMA table_info` AND observes them on a
    real inserted row (`full`, `1`, `0`).
  - Exercises the workspace -> world FK chain with uuid-allocated ids and asserts an FK rejection.
- Existing tests (`TestStoreRecordsScriptRunAndRevision`, `TestStoreReloadsInlineRevisionBlob`,
  `TestStoreLookupMissingRevision`, `TestStoreRejectsRevisionWithMissingScriptRun`) remain unchanged
  and green — proving the additive columns/tables don't perturb the existing insert/scan paths.

## Minor (non-blocking)
- The comment "A world_id referencing a non-existent world must be rejected by the FK" labels an
  insert that actually supplies a bad `workspace_id` (inserting into `worlds`). The assertion is still
  a valid, real FK-rejection test (`worlds.workspace_id REFERENCES workspaces ON DELETE RESTRICT`);
  only the comment is mislabeled. No coverage or correctness impact — not worth a round-trip.

## Verification
- `go test -count=1 ./revisionstore/...` — all 5 tests PASS (new test runs, not cached).
- `go test ./...` — all packages ok.
- `go build ./...` — OK. `go vet ./revisionstore/...` — clean.

Implementation matches the plan exactly, the test is real and load-bearing, scope is respected, and
the full suite is green.

VERDICT: PASS
