# E1 Review — Workspace automation Starlark globals

Reviewer: independent review pass (clean first review; prior run wrote no artifact).
Verdict basis: spec `docs/workspace_plan.md#e1`, plan `docs/tickets/E1.plan.md`, diff
`main...HEAD`, full suite re-run.

## Scope discipline (staked #1) — PASS
`git diff main...HEAD --name-status` shows exactly two additions:
`workspace/automation.go`, `workspace/automation_test.go`. No existing Go file edited;
nothing under `script/thebibites/` or `script/` touched. The serialization point is
untouched. Single commit `e9ed7e6`. Working tree clean (only the reviewer's
`.current-ticket` marker is untracked).

## Spec vs plan fidelity — PASS
The spec's E1 code block lists `world.open()`, `world.query` (working-copy),
`workspace.transfer`, `workspace.gc` but tags each with its owning track (E2/F2/G3). The
plan (Approach step 5) makes a reasoned, documented narrowing: `world.query` working-copy
read is deferred to E2 because the read path (`ls.query`) is unexported in `thebibites`
and reaching it would breach the serialization-point rule. This is a faithful deferral
consistent with the spec's track tags, not a silent drop. `world.history_query` (C4) and
`workspace.query` (C4) are the only query bindings shipped — matches both spec and plan.

## E1/E2/F2/G3 boundary (staked #2) — PASS
Implemented: world.history_query, workspace.query, node lifecycle
(info/state/stop/resume/reload/ingest_autosave/kill), worlds/world/add_world,
nodes/node/start_node, world.load/unload/evict_history. NOT implemented: world.open,
s.commit, working-copy world.query (E2), transfer (F2), gc (G3). Confirmed absent from
both Attr switches.

## Deferred-name footgun (staked #3) — PASS
`Attr` returns `(nil, nil)` for deferred names. Verified against the in-use starlark
version (`v0.0.0-20260324133313-ffb3f39dd27a`, eval.go getAttr): `(nil, nil)` from
`HasAttrs.Attr` yields a real error "`<type> has no .<name> field or method`" — it does
NOT silently produce None. `TestAutomation_DeferredNamesAbsent` asserts `err != nil` for
`world.open()`, `workspace.transfer(...)`, `workspace.gc()` (all three pass). No footgun.

## Error surfacing (staked #4) — PASS
Every builtin returns `(nil, err)` on failure (wrapped with a stable prefix), never
swallows, never panics. `script.Run` propagates the binding error to the returned `error`
(RunError). Proven by passing tests:
- `TestAutomation_ErrNotRematerializable_SurfacesCleanly` — reload on a forced mirror_only
  head; message reaches the script and mentions mirror_only/rematerializ.
- `TestAutomation_ErrReadOnlyQuery_SurfacesCleanly` and the DELETE half of
  `TestAutomation_WorkspaceQueryReadOnly` — non-SELECT rejected with the read-only message.
- `TestAutomation_WorldNotFound` — clean "not found" via `revisionstore.IsNotFound`.

## Conversion fidelity (staked #5) — PASS
`sqlScalarToStarlark` is a faithful line-for-line mirror of
`script/thebibites/convert.go:fromSQLValue`, including the `duckdb.NormalizeSQLScanValue`
delegation, the nil/NULL->None case, bool/string/int64/uint64/float64/*big.Int cases (with
the *big.Int nil guard) and the identical default error. Re-implemented locally as required
(no thebibites export). Dict keys are stable snake_case and match the plan's named contract
and the typed result structs verified field-by-field
(InfoResult/AutosaveInfo/StopResult/ResumeResult/ReloadResult/NodeState/EvictResult) and
revisionstore.World/Node/Revision field names. Query results map to a List of Dicts per row.

## ctx threading / locking — PASS
Run ctx is captured in the per-run value graph (workspaceValue/worldValue/nodeValue) and
passed to every Go method; no `context.Background()` inside bindings. All bindings go
through public `*Workspace` methods (each manages `w.mu` itself); no binding reaches
`w.mu`/`w.duck()` directly and none holds a lock across calls.

## Tests (staked #6) — PASS
11 real test functions exercise the bindings end-to-end against temp workspaces and the
existing net.Pipe fake nodes (newFakeNode/newReloadFakeNode), not stubs. Telemetry
assertions match the fake's values (tps 60.0, paused True, last_autosave name,
previous_time_scale 3.5, time_scale 2.0). Structural facts are cross-checked Go-side
(ListWorlds, head advance, 2 revisions, mirror_only tier).
Re-ran independently: `go test ./workspace/ -run TestAutomation` = 11/11 PASS (49.9s);
full `go test ./workspace/ -count=1` = ok (437.6s, confirms the slow-package note);
`go build ./...` and `go vet ./workspace/` clean; full `go test ./...` = all ok.

Minor (non-blocking) observations, not required fixes:
- A few success-path assertions in `TestAutomation_WorkspaceQueryReadOnly` and
  `TestAutomation_WorldHistoryQuery` only `t.Logf` on a 0/empty count rather than failing;
  the round-trip is still proven because the program would error if indexing failed, and
  the load-bearing rejection/error halves are hard-asserted.
- The `world.open()` message check in `TestAutomation_DeferredNamesAbsent` only `t.Logf`s
  the message text, but the load-bearing `err != nil` assertion is hard.

## Perf lens
No Perf-review flag; thin per-invocation passthrough over already-fixed `*Workspace`
methods. No new loop over growing data, no N+1, no hot-path allocation introduced. Skipped
as warranted.

VERDICT: PASS
